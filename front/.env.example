# Dev Docker : `./scripts/run.sh` + compose injectent déjà VITE_* depuis la racine — ne mets pas un second jeton ici.
# Cesium : définis uniquement CESIUM_ION_TOKEN dans le `.env` à la racine du monorepo.
# Hors Docker uniquement : VITE_API_BASE_URL=http://localhost:3000
